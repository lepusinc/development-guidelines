# リポジトリ

このドキュメントでは、Laravel アプリケーションにおけるリポジトリ層の設計に関するガイドラインを定めます。

## クエリの取得方法

- **[厳守]** `->get()`（全件取得）と `->paginate()`（ページネーション取得）のどちらを使うかは、呼び出し側（コントローラーやサービス層）が判断する。リポジトリ層で「ページネーションする版」「しない版」を別々のメソッドとして用意しない。

  実装例・理由は [サービス](./SERVICE.md#クエリの取得方法) を参照（同一ルールがリポジトリ・サービス両層に適用される）。
